<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NASAApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:fontFamily">@font/inter_semibold</item>
        <item name="colorPrimary">@color/nasa_blue</item>
    </style>
    <style name="TextAppearance.Quote" parent="TextAppearance.MaterialComponents.Headline1">
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">#073b4c</item>
        <item name="android:padding">8dp</item>
    </style>
    <style name="Image.Theme.NASAApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:fontFamily">@font/inter_semibold</item>
        <item name="android:background">@color/nasa_background</item>
        <item name="android:textColor">@color/white</item>
    </style>
    <style name="Theme.NASAApp" parent="Base.Theme.NASAApp" />
</resources>